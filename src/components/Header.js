import React from "react";
import styled from "styled-components";

const Header = () => {
  return <Container>Cosee Image Gallery</Container>;
};

const Container = styled.div`
  width: 100%;
  text-align: center;
`;

export default Header;
